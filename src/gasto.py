class Gasto:
    def __init__(self, fecha, descripcion, monto, categoria):
        self.fecha = fecha
        self.descripcion = descripcion
        self.monto = monto
        self.categoria = categoria